{% extends 'base.html.twig' %}

{% block title %}Add Item — Wishlist{% endblock %}

{% block body %}
    <div class="mb-4">
        <h1 class="text-2xl font-bold">Add new person</h1>
    </div>

    <div class="bg-white shadow rounded p-6 max-w-xl">
        {{ form_start(form) }}
        <div class="mb-4">
            {{ form_label(form.name) }}
            {{ form_widget(form.name, {'attr': {'class': 'w-full border rounded px-3 py-2'}}) }}
            {{ form_errors(form.name) }}
        </div>
        <div class="flex justify-end">
            <button type="submit" class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">Add Person</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
