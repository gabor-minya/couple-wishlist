<!DOCTYPE html>
<html lang="{{ app.request.locale ?: 'en' }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{% block title %}Wishlist{% endblock %}</title>

    {# Tailwind CDN for dev (replace with build pipeline later) #}
    <script src="https://cdn.tailwindcss.com"></script>

    {% block styles %}{% endblock %}
</head>
<body class="bg-gray-50 text-gray-900">
<nav class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4 py-3 flex justify-between items-center">
        <div class="flex items-center space-x-4">
            <a href="{{ path('dashboard') }}" class="text-xl font-semibold">Wishlist</a>
        </div>
        <div class="flex items-center space-x-3">
            <a href="#" class="text-sm">EN</a>
            <a href="#" class="text-sm">HU</a>
            {% if app.user %}
                <span class="text-sm">Hi, {{ app.user.username }}</span>
                <a href="{{ path('logout') }}" class="ml-2 text-sm text-red-600">Logout</a>
            {% endif %}
        </div>
    </div>
</nav>

<main class="max-w-7xl mx-auto p-4">
    {% block body %}{% endblock %}
</main>

{% block scripts %}{% endblock %}
</body>
</html>
